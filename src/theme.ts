import { ThemeConfig } from "antd";

const config: ThemeConfig = {
  token: {
    // colorPrimary: "#1890ff",
  },
  components: {
    Layout: {
      // "headerBg": "rgb(7, 128, 242)",
      headerColor: "#FFF",
    },
    Statistic: {
      contentFontSize: 14,
      marginXXS: 0,
    },
  },
};

export default config;
